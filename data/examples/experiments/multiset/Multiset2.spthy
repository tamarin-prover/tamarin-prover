theory Multiset2 begin

builtins: multiset


rule A:
  [ Fr(~x), Fr(~y), In(z) ] --[]-> [ Out(~x + ~y + z), S(~x) ]

rule B:
  [ S(~x), In(~x) ] --[ Reach() ]-> []


lemma Reachable:
  exists-trace
  "Ex #i. Reach() @ i"

end