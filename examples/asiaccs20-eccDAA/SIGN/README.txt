This is the first batch of models. More to follow once the code has been properly cleaned up.

Instruction on how to run each model is included in each file together with the timings it took on our server (32 CPU, 384GB RAM).

