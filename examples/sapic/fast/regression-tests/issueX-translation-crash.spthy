theory Typing
begin

builtins: diffie-hellman

let Client(sk: secretkey) =
    new a;
    let gA = g^A in
    out(<'helloClient',gA >);
    /* in(<'helloServer',=gA, gB>); // crashes unless we comment this out. */
    in(<'helloServer',=a, =gB>); 
    event Key(gB^a);
    0

process:
    new sk;
    Client(sk)

end
