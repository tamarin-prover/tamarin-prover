theory Patterns
begin


process:
in(x); // allowed
in(=x); // allowed
/* in(<=x,x>); // disallowed because ambigous */
/* in(x); // disallowed because x is bound in top-level process */
in(<y,y>); // allowed 
0


end
