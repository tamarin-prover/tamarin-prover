theory EncryptionComparison
begin

// Test builtin symmetric encryption. Should derive properly without any warnings.
builtins: symmetric-encryption

rule builtin_encrypt_decrypt:
  [ In(senc(x,k)), In(k) ]
  --[ BuiltinDecrypted(x) ]->
  [ Out(x) ]

end