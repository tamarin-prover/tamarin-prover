theory why
begin

builtins: xor, hashing

rule one:
  [ Fr(~x), Fr(~left), Fr(~right) ]
  --[ ]->
  [ Out(diff(~x XOR 'c1',~x XOR 'c2')) ] 
//  [ Out(h(<~x,diff('c1','c2')>)) ] 
//  [ Out(diff(~x XOR ~left,~x XOR ~right)) ] 

end

