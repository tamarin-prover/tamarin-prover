theory SignTest
begin

builtins: signing
functions: checksign/2
equations: verify( sign(m,k), m, pk(k) ) = true//, checksign( sign(m,k), pk(k) ) = m


rule R_1:
  [ Fr(~a), Fr(~b) ] --[ ]-> [ Out( pk(~a) ), Out( pk(~b) ), Out( sign($M,diff(~a,~b)) ) ]


end
