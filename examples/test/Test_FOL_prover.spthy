theory Test_FOL_prover begin

lemma imp1:
  "v :> In(<x, z*y>) ==> Ex u. u = v & Ex m1 m2. u :> In(<m1, m2>)"

lemma cycle:
  "not(v >+> u & u >+> w & w >+> v)"

lemma AC1:
  "Ex u a b w x. <v * (h(w) * x),a> = <b,w * v * h(u)>"

lemma AC2:
  "Ex a b c d. a * b * c * d = d * (c * (b * a))"


end
