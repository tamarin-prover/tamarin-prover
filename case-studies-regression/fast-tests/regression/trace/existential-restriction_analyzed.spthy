theory ExistentialRestriction

begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, snd/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

rule (modulo E) Start:
   [ Fr( ~x ) ] --[ A( ~x ) ]-> [ Pending( ~x ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Finish:
   [ Pending( x ) ] --[ B( x ) ]-> [ ]

  /* has exactly the trivial AC variant */

restriction finish_after_start:
  "∀ x #i. (A( x ) @ #i) ⇒ (∃ #j. (B( x ) @ #j) ∧ (#i < #j))"

  /*
  expanded formula:
  "∀ x #i. (A( x ) @ #i) ⇒ (∃ #j. (B( x ) @ #j) ∧ (#i < #j))"
  */

lemma reachable:
  exists-trace "∃ x #i. (A( x ) @ #i) ∧ (∃ #j. (B( x ) @ #j) ∧ (#i < #j))"
/*
guarded formula characterizing all satisfying traces:
"∃ x #i. (A( x ) @ #i) ∧ ∃ #j. (B( x ) @ #j) ∧ #i < #j"
*/
simplify
solve( Pending( ~x ) ▶₀ #j )
  case Start
  SOLVED // trace found
qed

lemma restriction_applies:
  all-traces "∀ x #i. (A( x ) @ #i) ⇒ (∃ #j. (B( x ) @ #j) ∧ (#i < #j))"
/*
guarded formula characterizing all counter-examples:
"∃ x #i. (A( x ) @ #i) ∧ ∀ #j. (B( x ) @ #j) ⇒ ¬(#i < #j)"
*/
simplify
by contradiction /* from formulas */

/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: e21ed2902accbb12f7062569d046b00b1799386a, branch: fix-guarded-singleton-connectives
Compiled at: 2026-09-14 15:10:54.267940289 UTC
*/

end
/* Output

==============================================================================
summary of summaries:

analyzed: examples/regression/trace/existential-restriction.spthy

  output:          examples/regression/trace/existential-restriction.spthy.tmp
  processing time: 0.04s
  
  reachable (exists-trace): verified (3 steps)
  restriction_applies (all-traces): verified (2 steps)

==============================================================================
*/
